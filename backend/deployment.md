# Deployment

not deployed due to time constraints

next steps for deployment: push to Render.com, add DATABASE_URL as an environment variable, set build command to `npm install && npx prisma generate` and start command to `node server.js`